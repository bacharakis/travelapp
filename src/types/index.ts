export interface FlightInfo {
    flightNumber: string;
    flightDate: string;
    status: string;
    terminal: string;
    gate: string;
    departureTime: string;
    arrivalTime: string;
    airline: string;
    aircraft: string;
}